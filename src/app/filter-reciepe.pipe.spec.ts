import { FilterReciepePipe } from './filter-reciepe.pipe';

describe('FilterReciepePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReciepePipe();
    expect(pipe).toBeTruthy();
  });
});
